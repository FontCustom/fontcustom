require 'spec_helper'
require 'fontcustom/cli'

describe Fontcustom::CLI do
  describe '#compile' do
    it 'must be able to compile' do
      skip 'Not ready'
      puts `bundle exec bin/fontcustom compile demo`
    end
  end
end
